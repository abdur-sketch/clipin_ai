import { bindings,currentUser,guardMutation } from "@/lib/server";

export async function GET(){const user=await currentUser();const {results}=await bindings.DB.prepare("SELECT * FROM workspace_members WHERE owner_id=? ORDER BY created_at DESC").bind(user.id).all();return Response.json({members:results})}
export async function POST(request:Request){const guarded=guardMutation(request,"team-member");if(guarded)return guarded;const user=await currentUser();const body=await request.json() as {email?:string;role?:string};const email=String(body.email||"").trim().toLowerCase();const role=["editor","reviewer","viewer"].includes(String(body.role))?String(body.role):"reviewer";if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return Response.json({error:"Email tidak valid"},{status:400});const now=Date.now();await bindings.DB.prepare("INSERT INTO workspace_members (id,owner_id,email,role,status,created_at,updated_at) VALUES (?,?,?,?,?,?,?)").bind(crypto.randomUUID(),user.id,email,role,"invited",now,now).run();return Response.json({ok:true})}
export async function DELETE(request:Request){const guarded=guardMutation(request,"team-member-delete");if(guarded)return guarded;const user=await currentUser();const id=new URL(request.url).searchParams.get("id")||"";await bindings.DB.prepare("DELETE FROM workspace_members WHERE id=? AND owner_id=?").bind(id,user.id).run();return Response.json({ok:true})}
